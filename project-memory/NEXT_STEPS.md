# Next steps

1. Evaluate and integrate a mature BrightScript runtime adapter; keep the current subset explicitly experimental until replaced.
2. Reduce the 49 IEDB compatibility warnings by adding observers, inheritance, focus geometry, Roku API components, and more compatibility fixtures.
3. Add macOS/Linux artifacts and GitHub release automation.
4. Add source diagnostics and language-server integration to Monaco.
5. Authenticate GitHub CLI, push the local commits, and publish alpha.2 with the validated Windows artifact.
